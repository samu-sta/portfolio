---
export interface Props {
  class?: string;
  style?: string;
}

const { class: className, style } = Astro.props;
---

<article 
  class={`rounded-2xl backdrop-blur-sm overflow-hidden  ${className || ''}`}
  style={style}
>
  <slot />
</article>